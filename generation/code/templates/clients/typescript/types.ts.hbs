// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Source: {{sourceFile}}
//
//     TypeScript DTO interfaces for the {{contextName}} bounded context.
//     These mirror the backend C# DTOs and are safe to use in frontend/client code.
// </auto-generated>

{{#each entities}}
// ─────────────────────────────────────────────────────────────────────────────
// {{entityName}}
// ─────────────────────────────────────────────────────────────────────────────

export interface {{createDtoName}} {
{{#each createProps}}
  {{name}}{{#if optional}}?{{/if}}: {{tsType}};
{{/each}}
}

export interface {{updateDtoName}} {
{{#each updateProps}}
  {{name}}{{#if optional}}?{{/if}}: {{tsType}};
{{/each}}
}

export interface {{deleteDtoName}} {
{{#each deleteProps}}
  {{name}}{{#if optional}}?{{/if}}: {{tsType}};
{{/each}}
}

export interface {{detailsDtoName}} {
{{#each detailsProps}}
  {{name}}{{#if optional}}?{{/if}}: {{tsType}};
{{/each}}
}

/** Search / filter DTO. All fields are optional filters; results are paged. */
export interface {{searchDtoName}} {
{{#each searchProps}}
  {{name}}?: {{tsType}};
{{/each}}
  page?: number;
  pageSize?: number;
}

export interface {{projectionName}} {
{{#each projectionProps}}
  {{name}}{{#if optional}}?{{/if}}: {{tsType}};
{{/each}}
}

{{/each}}

// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Source: {{sourceFile}}
// </auto-generated>

import type {
  {{createDtoName}},
  {{updateDtoName}},
  {{deleteDtoName}},
  {{detailsDtoName}},
  {{searchDtoName}},
  {{projectionName}},
} from './types';

export class {{entityName}}Client {
  constructor(private readonly baseUrl: string) {}

  private request<T>(method: string, path: string, body?: unknown): Promise<T> {
    return new Promise((resolve, reject) => {
      const xhr = new XMLHttpRequest();
      xhr.open(method, `${this.baseUrl}/${path}`);
      xhr.setRequestHeader('Content-Type', 'application/json');
      xhr.setRequestHeader('Accept', 'application/json');
      xhr.onload = () => {
        if (xhr.status === 204) { resolve(undefined as unknown as T); return; }
        if (xhr.status === 404) { reject(Object.assign(new Error('Not found'), { status: 404 })); return; }
        if (xhr.status >= 200 && xhr.status < 300) {
          try { resolve(JSON.parse(xhr.responseText) as T); }
          catch { resolve(xhr.responseText as unknown as T); }
        } else {
          reject(new Error(`HTTP ${xhr.status}: ${xhr.statusText}`));
        }
      };
      xhr.onerror = () => reject(new Error('Network error'));
      xhr.send(body !== undefined ? JSON.stringify(body) : null);
    });
  }

  /** List / search {{entityNamePlural}} */
  async getAll(search?: {{searchDtoName}}): Promise<{{projectionName}}[]> {
    const params = search
      ? '?' + new URLSearchParams(Object.entries(search ?? {}).map(([k, v]) => [k, String(v)])).toString()
      : '';
    return this.request<{{projectionName}}[]>('GET', `{{routePrefix}}${params}`);
  }

  /** Get a single {{entityName}} by primary key */
  async getById({{pkNameLower}}: {{pkTsType}}): Promise<{{detailsDtoName}} | null> {
    try {
      return await this.request<{{detailsDtoName}}>(
        'GET',
        `{{routePrefix}}/${encodeURIComponent(String({{pkNameLower}}))}`
      );
    } catch (err: unknown) {
      if ((err as { status?: number }).status === 404) return null;
      throw err;
    }
  }

  /** Create a new {{entityName}} */
  async create(dto: {{createDtoName}}): Promise<{{detailsDtoName}}> {
    return this.request<{{detailsDtoName}}>('POST', '{{routePrefix}}', dto);
  }

  /** Update an existing {{entityName}} */
  async update(dto: {{updateDtoName}}): Promise<{{detailsDtoName}}> {
    return this.request<{{detailsDtoName}}>(
      'PUT',
      `{{routePrefix}}/${encodeURIComponent(String(dto.{{pkNameLower}}))}`,
      dto
    );
  }

  /** Delete a {{entityName}} */
  async delete(dto: {{deleteDtoName}}): Promise<void> {
    await this.request<void>(
      'DELETE',
      `{{routePrefix}}/${encodeURIComponent(String(dto.{{pkNameLower}}))}`,
      dto
    );
  }
}

// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Source: {{sourceFile}}
// </auto-generated>

import axios, { AxiosInstance } from 'axios';
import type {
  {{createDtoName}},
  {{updateDtoName}},
  {{deleteDtoName}},
  {{detailsDtoName}},
  {{searchDtoName}},
  {{projectionName}},
} from './types';

export class {{entityName}}Client {
  private readonly http: AxiosInstance;

  constructor(baseUrl: string, instance?: AxiosInstance) {
    this.http = instance ?? axios.create({ baseURL: baseUrl });
  }

  /** List / search {{entityNamePlural}} */
  async getAll(search?: {{searchDtoName}}): Promise<{{projectionName}}[]> {
    const { data } = await this.http.get<{{projectionName}}[]>('/{{routePrefix}}', {
      params: search,
    });
    return data;
  }

  /** Get a single {{entityName}} by primary key */
  async getById({{pkNameLower}}: {{pkTsType}}): Promise<{{detailsDtoName}} | null> {
    try {
      const { data } = await this.http.get<{{detailsDtoName}}>(
        `/{{routePrefix}}/${encodeURIComponent(String({{pkNameLower}}))}`
      );
      return data;
    } catch (err: unknown) {
      if (axios.isAxiosError(err) && err.response?.status === 404) return null;
      throw err;
    }
  }

  /** Create a new {{entityName}} */
  async create(dto: {{createDtoName}}): Promise<{{detailsDtoName}}> {
    const { data } = await this.http.post<{{detailsDtoName}}>('/{{routePrefix}}', dto);
    return data;
  }

  /** Update an existing {{entityName}} */
  async update(dto: {{updateDtoName}}): Promise<{{detailsDtoName}}> {
    const { data } = await this.http.put<{{detailsDtoName}}>(
      `/{{routePrefix}}/${encodeURIComponent(String(dto.{{pkNameLower}}))}`,
      dto
    );
    return data;
  }

  /** Delete a {{entityName}} */
  async delete(dto: {{deleteDtoName}}): Promise<void> {
    await this.http.delete(
      `/{{routePrefix}}/${encodeURIComponent(String(dto.{{pkNameLower}}))}`
    );
  }
}

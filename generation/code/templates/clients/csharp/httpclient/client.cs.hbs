// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Source: {{sourceFile}}
// </auto-generated>

using System.Net;
using System.Net.Http.Json;
using {{namespace}}.Dto;

namespace {{namespace}};

/// <summary>Contract for <see cref="{{entityName}}HttpClient" />.</summary>
public interface I{{entityName}}Client
{
    Task<IReadOnlyList<{{projectionName}}>> GetAllAsync({{searchDtoName}}? search = null, CancellationToken ct = default);
    Task<{{detailsDtoName}}?> GetByIdAsync({{pkCsType}} {{pkNameLower}}, CancellationToken ct = default);
    Task<{{detailsDtoName}}> CreateAsync({{createDtoName}} dto, CancellationToken ct = default);
    Task<{{detailsDtoName}}> UpdateAsync({{pkCsType}} {{pkNameLower}}, {{updateDtoName}} dto, CancellationToken ct = default);
    Task DeleteAsync({{pkCsType}} {{pkNameLower}}, CancellationToken ct = default);
}

/// <summary>HttpClient-based implementation of <see cref="I{{entityName}}Client" />.</summary>
public sealed class {{entityName}}HttpClient : I{{entityName}}Client
{
    private readonly HttpClient _http;

    public {{entityName}}HttpClient(HttpClient http) => _http = http;

    /// <inheritdoc />
    public async Task<IReadOnlyList<{{projectionName}}>> GetAllAsync(
        {{searchDtoName}}? search = null, CancellationToken ct = default)
    {
        var url = "/{{routePrefix}}" + ToQueryString(search);
        return await _http.GetFromJsonAsync<IReadOnlyList<{{projectionName}}>>(url, ct) ?? [];
    }

    /// <inheritdoc />
    public async Task<{{detailsDtoName}}?> GetByIdAsync({{pkCsType}} {{pkNameLower}}, CancellationToken ct = default)
    {
        var response = await _http.GetAsync($"{{routeWithPkVar}}", ct);
        if (response.StatusCode == HttpStatusCode.NotFound) return null;
        response.EnsureSuccessStatusCode();
        return await response.Content.ReadFromJsonAsync<{{detailsDtoName}}>(cancellationToken: ct);
    }

    /// <inheritdoc />
    public async Task<{{detailsDtoName}}> CreateAsync({{createDtoName}} dto, CancellationToken ct = default)
    {
        var response = await _http.PostAsJsonAsync("/{{routePrefix}}", dto, ct);
        response.EnsureSuccessStatusCode();
        return (await response.Content.ReadFromJsonAsync<{{detailsDtoName}}>(cancellationToken: ct))!;
    }

    /// <inheritdoc />
    public async Task<{{detailsDtoName}}> UpdateAsync({{pkCsType}} {{pkNameLower}}, {{updateDtoName}} dto, CancellationToken ct = default)
    {
        var response = await _http.PutAsJsonAsync($"/{{routePrefix}}/{{{pkNameLower}}}", dto, ct);
        response.EnsureSuccessStatusCode();
        return (await response.Content.ReadFromJsonAsync<{{detailsDtoName}}>(cancellationToken: ct))!;
    }

    /// <inheritdoc />
    public async Task DeleteAsync({{pkCsType}} {{pkNameLower}}, CancellationToken ct = default)
    {
        var response = await _http.DeleteAsync($"/{{routePrefix}}/{{{pkNameLower}}}", ct);
        response.EnsureSuccessStatusCode();
    }

    private static string ToQueryString(object? obj)
    {
        if (obj is null) return string.Empty;
        var props = obj.GetType().GetProperties()
            .Where(p => p.GetValue(obj) is not null)
            .Select(p =>
                $"{Uri.EscapeDataString(p.Name)}={Uri.EscapeDataString(p.GetValue(obj)!.ToString()!)}");
        var qs = string.Join("&", props);
        return qs.Length == 0 ? string.Empty : "?" + qs;
    }
}

// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Source: {{sourceFile}}
//
//     Standalone C# DTO records for the {{contextName}} bounded context client.
//     These mirror the backend DTOs but have no dependency on the server-side projects.
// </auto-generated>

namespace {{namespace}};

{{#each entities}}
// ─────────────────────────────────────────────────────────────────────────────
// {{entityName}} DTOs
// ─────────────────────────────────────────────────────────────────────────────

public sealed record {{createDtoName}}
{
{{#each createProps}}
{{#if optional}}
    public {{csType}} {{name}} { get; init; } = default!;
{{else}}
    public required {{csType}} {{name}} { get; init; }
{{/if}}
{{/each}}
}

public sealed record {{updateDtoName}}
{
{{#each updateProps}}
{{#if optional}}
    public {{csType}} {{name}} { get; init; } = default!;
{{else}}
    public required {{csType}} {{name}} { get; init; }
{{/if}}
{{/each}}
}

public sealed record {{deleteDtoName}}
{
{{#each deleteProps}}
    public required {{csType}} {{name}} { get; init; }
{{/each}}
}

public sealed record {{detailsDtoName}}
{
{{#each detailsProps}}
{{#if optional}}
    public {{csType}} {{name}} { get; init; } = default!;
{{else}}
    public required {{csType}} {{name}} { get; init; }
{{/if}}
{{/each}}
}

/// <summary>Search / filter DTO. All fields are optional filters; results are paged.</summary>
public sealed record {{searchDtoName}}
{
{{#each searchProps}}
    public {{csType}} {{name}} { get; init; } = default!;
{{/each}}
    public int? Page { get; init; }
    public int? PageSize { get; init; }
}

public sealed record {{projectionName}}
{
{{#each projectionProps}}
{{#if optional}}
    public {{csType}} {{name}} { get; init; } = default!;
{{else}}
    public required {{csType}} {{name}} { get; init; }
{{/if}}
{{/each}}
}

{{/each}}

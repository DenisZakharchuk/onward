// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Source: {{sourceFile}}
// </auto-generated>

using Microsoft.Extensions.DependencyInjection;
using {{namespace}}.Dto;
{{#if isRefit}}
using Refit;
{{/if}}

namespace {{namespace}};

/// <summary>
/// Extension methods for registering {{contextName}} HTTP clients with the DI container.
/// </summary>
public static class {{contextName}}ClientServiceCollectionExtensions
{
    /// <summary>
    /// Registers all {{contextName}} HTTP clients, pointing at <paramref name="baseUrl" />.
    /// </summary>
    public static IServiceCollection Add{{contextName}}HttpClients(
        this IServiceCollection services,
        string baseUrl)
    {
{{#if isHttpClient}}
{{#each entities}}
        services.AddHttpClient<I{{entityName}}Client, {{entityName}}HttpClient>(client =>
            client.BaseAddress = new Uri(baseUrl));

{{/each}}
{{/if}}
{{#if isRefit}}
{{#each entities}}
        services
            .AddRefitClient<I{{entityName}}Client>()
            .ConfigureHttpClient(c => c.BaseAddress = new Uri(baseUrl));

{{/each}}
{{/if}}
        return services;
    }
}

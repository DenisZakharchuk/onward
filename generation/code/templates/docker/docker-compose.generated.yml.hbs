# <auto-generated>
#     This file was generated by the BoundedContext Code Generator.
#     Generation Stamp: {{generationStamp}}
#     Generated: {{generatedAt}}
#     Source: {{sourceFile}}
#     Context: {{contextName}}
#
#     DO NOT edit manually — re-run the generator to update.
# </auto-generated>

services:
  {{dbmsServiceName}}:
    image: {{dbmsImage}}
    container_name: {{dbmsServiceName}}
    restart: unless-stopped
    ports:
      - "{{dbPort}}:{{dbmsContainerPort}}"
    environment:
{{#each envVars}}
      {{this.key}}: {{this.value}}
{{/each}}
    volumes:
      - {{volumeName}}:{{dbmsDataPath}}
    networks:
      - {{networkName}}
    healthcheck:
      test: {{{healthcheck}}}
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  {{volumeName}}:
    driver: local

networks:
  {{networkName}}:
    external: true

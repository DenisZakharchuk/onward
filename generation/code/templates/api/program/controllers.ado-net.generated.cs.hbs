// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Generation Stamp: {{generationStamp}}
//     Generated: {{generatedAt}}
//     Source: {{sourceFile}}
// </auto-generated>

using Microsoft.OpenApi.Models;
using Onward.Base.AspNetCore.Extensions;
using {{namespace}}.DI;

var builder = WebApplication.CreateBuilder(args);

var connectionString = builder.Configuration.GetConnectionString("{{contextName}}Database")
    ?? throw new InvalidOperationException("Connection string '{{contextName}}Database' not found.");

builder.Services.Add{{contextName}}Services(connectionString);

// ===== Authentication =====
{{#if authorizationEnabled}}
builder.Services.AddOnwardJwtAuth(builder.Configuration);
{{else}}
builder.Services.AddOnwardAnonymousAuth();
{{/if}}

builder.Services.AddControllers();
builder.Services.AddEndpointsApiExplorer();
builder.Services.AddSwaggerGen(c =>
{
    c.SwaggerDoc("v1", new OpenApiInfo
    {
        Title = "{{contextName}} API",
        Version = "v1",
        Description = "{{contextDescription}}"
    });
    var xmlFile = $"{System.Reflection.Assembly.GetExecutingAssembly().GetName().Name}.xml";
    var xmlPath = Path.Combine(AppContext.BaseDirectory, xmlFile);
    c.IncludeXmlComments(xmlPath);
{{#if authorizationEnabled}}
    c.AddOnwardJwtSecurity();
{{/if}}
});

var app = builder.Build();

if (app.Environment.IsDevelopment())
{
    app.UseSwagger();
    app.UseSwaggerUI();
}

app.UseOnwardAuth();
app.MapControllers();

app.Run();

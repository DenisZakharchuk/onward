// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Generation Stamp: {{generationStamp}}
//     Generated: {{generatedAt}}
//     Source: {{sourceFile}}
// </auto-generated>

using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using System.Security.Claims;
using {{namespace}}.BL.Services.Abstractions;
using {{namespace}}.DTO.Auth;
using Onward.Base.DTOs;

namespace {{namespace}}.API.Controllers;

/// <summary>
/// Authentication endpoints for the {{contextName}} bounded context (perContext auth mode).
/// Provides login, token refresh, and logout — handled independently of Onward.Auth.
/// </summary>
[ApiController]
[Route("api/[controller]")]
public sealed class AuthController : ControllerBase
{
    private readonly I{{contextName}}AuthenticationService _authService;
    private readonly ILogger<AuthController> _logger;

    public AuthController(
        I{{contextName}}AuthenticationService authService,
        ILogger<AuthController> logger)
    {
        _authService = authService;
        _logger = logger;
    }

    /// <summary>Authenticate a user and return a JWT access token + refresh token.</summary>
    [HttpPost("login")]
    [AllowAnonymous]
    public async Task<ActionResult<ServiceResult<{{contextName}}LoginResponseDTO>>> Login(
        [FromBody] {{contextName}}LoginRequestDTO request,
        CancellationToken cancellationToken = default)
    {
        var ipAddress = HttpContext.Connection.RemoteIpAddress?.ToString() ?? "unknown";
        _logger.LogInformation("Login attempt for {Email} from {IpAddress}", request.Email, ipAddress);

        var result = await _authService.LoginAsync(request.Email, request.Password, ipAddress, cancellationToken);
        return result.IsSuccess ? Ok(result) : BadRequest(result);
    }

    /// <summary>Exchange a valid refresh token for a new access token and refresh token pair.</summary>
    [HttpPost("refresh")]
    [AllowAnonymous]
    public async Task<ActionResult<ServiceResult<{{contextName}}LoginResponseDTO>>> Refresh(
        [FromBody] {{contextName}}RefreshTokenRequestDTO request,
        CancellationToken cancellationToken = default)
    {
        var ipAddress = HttpContext.Connection.RemoteIpAddress?.ToString() ?? "unknown";
        _logger.LogInformation("Token refresh from {IpAddress}", ipAddress);

        var result = await _authService.RefreshTokenAsync(request.RefreshToken, ipAddress, cancellationToken);
        return result.IsSuccess ? Ok(result) : BadRequest(result);
    }

    /// <summary>Revoke the current user's refresh token and invalidate the session.</summary>
    [HttpPost("logout")]
    [Authorize]
    public async Task<ActionResult<ServiceResult<bool>>> Logout(
        CancellationToken cancellationToken = default)
    {
        var userId = User.FindFirstValue(ClaimTypes.NameIdentifier);
        if (!Guid.TryParse(userId, out var userGuid))
            return Unauthorized(ServiceResult<bool>.Failure("Invalid token."));

        var result = await _authService.LogoutAsync(userGuid, cancellationToken);
        return result.IsSuccess ? Ok(result) : BadRequest(result);
    }
}

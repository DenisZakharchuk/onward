// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Generation Stamp: 20260411-7ded7da9
//     Generated: 2026-04-11 15:49:38 UTC
//     Source: procurement.json
//
//     Changes to this file will be lost when regenerated.
//     Custom business logic should be added to separate domain services.
// </auto-generated>

using Onward.Base.Models;

namespace EnergyBrockerage.Prozorro.BL.Entities;

/// <summary>
/// Qualification or technical criterion defined on a tender
/// </summary>
public class Criterion : BaseEntity
{
    /// <summary>
    /// Private parameterless constructor for EF Core
    /// </summary>
    private Criterion() { }

    /// <summary>
    /// Creates a new instance of Criterion
    /// </summary>
    /// <param name="externalId">Prozorro criterion ID</param>
    /// <param name="title">Criterion title</param>
    /// <param name="description">Criterion description</param>
    /// <param name="source">Criterion source (e.g. tenderer, procuringEntity)</param>
    /// <param name="relatesTo">Entity this criterion relates to (lot, item, tender)</param>
    /// <param name="relatedItem">External ID of the related item</param>
    /// <param name="classificationScheme">Classification scheme of the criterion</param>
    /// <param name="classificationId">Classification code of the criterion</param>
    /// <param name="tenderId">Parent tender</param>
    public Criterion(
        string externalId,
        string title,
        string description,
        string source,
        string relatesTo,
        string relatedItem,
        string classificationScheme,
        string classificationId,
        Guid tenderId
    )
    {
        if (string.IsNullOrWhiteSpace(externalId)) throw new ArgumentException("ExternalId cannot be empty", nameof(externalId));
        if (externalId.Length > 100) throw new ArgumentException("ExternalId cannot exceed 100 characters", nameof(externalId));
        if (title.Length > 500) throw new ArgumentException("Title cannot exceed 500 characters", nameof(title));
        if (description.Length > 2000) throw new ArgumentException("Description cannot exceed 2000 characters", nameof(description));
        if (source.Length > 100) throw new ArgumentException("Source cannot exceed 100 characters", nameof(source));
        if (relatesTo.Length > 100) throw new ArgumentException("RelatesTo cannot exceed 100 characters", nameof(relatesTo));
        if (relatedItem.Length > 100) throw new ArgumentException("RelatedItem cannot exceed 100 characters", nameof(relatedItem));
        if (classificationScheme.Length > 100) throw new ArgumentException("ClassificationScheme cannot exceed 100 characters", nameof(classificationScheme));
        if (classificationId.Length > 100) throw new ArgumentException("ClassificationId cannot exceed 100 characters", nameof(classificationId));

        ExternalId = externalId;
        Title = title;
        Description = description;
        Source = source;
        RelatesTo = relatesTo;
        RelatedItem = relatedItem;
        ClassificationScheme = classificationScheme;
        ClassificationId = classificationId;
        TenderId = tenderId;

        Id = Guid.NewGuid();
        CreatedAt = DateTime.UtcNow;
    }

    /// <summary>
    /// Prozorro criterion ID
    /// </summary>
    public string ExternalId { get; private set; }

    /// <summary>
    /// Criterion title
    /// </summary>
    public string Title { get; private set; }

    /// <summary>
    /// Criterion description
    /// </summary>
    public string Description { get; private set; }

    /// <summary>
    /// Criterion source (e.g. tenderer, procuringEntity)
    /// </summary>
    public string Source { get; private set; }

    /// <summary>
    /// Entity this criterion relates to (lot, item, tender)
    /// </summary>
    public string RelatesTo { get; private set; }

    /// <summary>
    /// External ID of the related item
    /// </summary>
    public string RelatedItem { get; private set; }

    /// <summary>
    /// Classification scheme of the criterion
    /// </summary>
    public string ClassificationScheme { get; private set; }

    /// <summary>
    /// Classification code of the criterion
    /// </summary>
    public string ClassificationId { get; private set; }

    /// <summary>
    /// Parent tender
    /// </summary>
    public Guid TenderId { get; private set; }

    /// <summary>
    /// Navigation to Tender
    /// </summary>
    public Tender Tender { get; } = null!;

    /// <summary>
    /// Collection of RequirementGroup
    /// </summary>
    public ICollection<RequirementGroup> RequirementGroups { get; } = new List<RequirementGroup>(10);

    /// <summary>
    /// Timestamp when the entity was created
    /// </summary>
    public DateTime CreatedAt { get; private set; }

    /// <summary>
    /// Updates Criterion with new values
    /// </summary>
    /// <param name="externalId">Prozorro criterion ID</param>
    /// <param name="title">Criterion title</param>
    /// <param name="description">Criterion description</param>
    /// <param name="source">Criterion source (e.g. tenderer, procuringEntity)</param>
    /// <param name="relatesTo">Entity this criterion relates to (lot, item, tender)</param>
    /// <param name="relatedItem">External ID of the related item</param>
    /// <param name="classificationScheme">Classification scheme of the criterion</param>
    /// <param name="classificationId">Classification code of the criterion</param>
    public void Update(
        string externalId,
        string title,
        string description,
        string source,
        string relatesTo,
        string relatedItem,
        string classificationScheme,
        string classificationId
    )
    {
        if (string.IsNullOrWhiteSpace(externalId)) throw new ArgumentException("ExternalId cannot be empty", nameof(externalId));
        if (externalId.Length > 100) throw new ArgumentException("ExternalId cannot exceed 100 characters", nameof(externalId));
        if (title.Length > 500) throw new ArgumentException("Title cannot exceed 500 characters", nameof(title));
        if (description.Length > 2000) throw new ArgumentException("Description cannot exceed 2000 characters", nameof(description));
        if (source.Length > 100) throw new ArgumentException("Source cannot exceed 100 characters", nameof(source));
        if (relatesTo.Length > 100) throw new ArgumentException("RelatesTo cannot exceed 100 characters", nameof(relatesTo));
        if (relatedItem.Length > 100) throw new ArgumentException("RelatedItem cannot exceed 100 characters", nameof(relatedItem));
        if (classificationScheme.Length > 100) throw new ArgumentException("ClassificationScheme cannot exceed 100 characters", nameof(classificationScheme));
        if (classificationId.Length > 100) throw new ArgumentException("ClassificationId cannot exceed 100 characters", nameof(classificationId));

        ExternalId = externalId;
        Title = title;
        Description = description;
        Source = source;
        RelatesTo = relatesTo;
        RelatedItem = relatedItem;
        ClassificationScheme = classificationScheme;
        ClassificationId = classificationId;

    }

    /// <summary>
    /// Parent tender
    /// </summary>
    /// <param name="tenderId">Tender identifier</param>
    public void SetTender(Guid tenderId)
    {
        TenderId = tenderId;
    }

}

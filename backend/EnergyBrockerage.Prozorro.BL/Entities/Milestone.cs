// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Generation Stamp: 20260411-7ded7da9
//     Generated: 2026-04-11 15:49:38 UTC
//     Source: procurement.json
//
//     Changes to this file will be lost when regenerated.
//     Custom business logic should be added to separate domain services.
// </auto-generated>

using Onward.Base.Models;

namespace EnergyBrockerage.Prozorro.BL.Entities;

/// <summary>
/// Payment or delivery milestone associated with a tender
/// </summary>
public class Milestone : BaseEntity
{
    /// <summary>
    /// Private parameterless constructor for EF Core
    /// </summary>
    private Milestone() { }

    /// <summary>
    /// Creates a new instance of Milestone
    /// </summary>
    /// <param name="externalId">Prozorro milestone ID</param>
    /// <param name="title">Milestone title</param>
    /// <param name="description">Milestone description</param>
    /// <param name="type">Milestone type (e.g. prepayment, postpayment)</param>
    /// <param name="code">Milestone code</param>
    /// <param name="percentage">Percentage of contract value for this milestone</param>
    /// <param name="durationDays">Duration in days</param>
    /// <param name="durationType">Duration type (working, banking, calendar)</param>
    /// <param name="sequenceNumber">Ordering sequence number</param>
    /// <param name="tenderId">Parent tender</param>
    public Milestone(
        string externalId,
        string title,
        string description,
        string type,
        string code,
        decimal? percentage,
        int? durationDays,
        string durationType,
        int? sequenceNumber,
        Guid tenderId
    )
    {
        if (string.IsNullOrWhiteSpace(externalId)) throw new ArgumentException("ExternalId cannot be empty", nameof(externalId));
        if (externalId.Length > 100) throw new ArgumentException("ExternalId cannot exceed 100 characters", nameof(externalId));
        if (title.Length > 500) throw new ArgumentException("Title cannot exceed 500 characters", nameof(title));
        if (description.Length > 2000) throw new ArgumentException("Description cannot exceed 2000 characters", nameof(description));
        if (type.Length > 100) throw new ArgumentException("Type cannot exceed 100 characters", nameof(type));
        if (code.Length > 100) throw new ArgumentException("Code cannot exceed 100 characters", nameof(code));
        if (durationType.Length > 50) throw new ArgumentException("DurationType cannot exceed 50 characters", nameof(durationType));

        ExternalId = externalId;
        Title = title;
        Description = description;
        Type = type;
        Code = code;
        Percentage = percentage;
        DurationDays = durationDays;
        DurationType = durationType;
        SequenceNumber = sequenceNumber;
        TenderId = tenderId;

        Id = Guid.NewGuid();
        CreatedAt = DateTime.UtcNow;
    }

    /// <summary>
    /// Prozorro milestone ID
    /// </summary>
    public string ExternalId { get; private set; }

    /// <summary>
    /// Milestone title
    /// </summary>
    public string Title { get; private set; }

    /// <summary>
    /// Milestone description
    /// </summary>
    public string Description { get; private set; }

    /// <summary>
    /// Milestone type (e.g. prepayment, postpayment)
    /// </summary>
    public string Type { get; private set; }

    /// <summary>
    /// Milestone code
    /// </summary>
    public string Code { get; private set; }

    /// <summary>
    /// Percentage of contract value for this milestone
    /// </summary>
    public decimal? Percentage { get; private set; }

    /// <summary>
    /// Duration in days
    /// </summary>
    public int? DurationDays { get; private set; }

    /// <summary>
    /// Duration type (working, banking, calendar)
    /// </summary>
    public string DurationType { get; private set; }

    /// <summary>
    /// Ordering sequence number
    /// </summary>
    public int? SequenceNumber { get; private set; }

    /// <summary>
    /// Parent tender
    /// </summary>
    public Guid TenderId { get; private set; }

    /// <summary>
    /// Navigation to Tender
    /// </summary>
    public Tender Tender { get; } = null!;

    /// <summary>
    /// Timestamp when the entity was created
    /// </summary>
    public DateTime CreatedAt { get; private set; }

    /// <summary>
    /// Updates Milestone with new values
    /// </summary>
    /// <param name="externalId">Prozorro milestone ID</param>
    /// <param name="title">Milestone title</param>
    /// <param name="description">Milestone description</param>
    /// <param name="type">Milestone type (e.g. prepayment, postpayment)</param>
    /// <param name="code">Milestone code</param>
    /// <param name="percentage">Percentage of contract value for this milestone</param>
    /// <param name="durationDays">Duration in days</param>
    /// <param name="durationType">Duration type (working, banking, calendar)</param>
    /// <param name="sequenceNumber">Ordering sequence number</param>
    public void Update(
        string externalId,
        string title,
        string description,
        string type,
        string code,
        decimal? percentage,
        int? durationDays,
        string durationType,
        int? sequenceNumber
    )
    {
        if (string.IsNullOrWhiteSpace(externalId)) throw new ArgumentException("ExternalId cannot be empty", nameof(externalId));
        if (externalId.Length > 100) throw new ArgumentException("ExternalId cannot exceed 100 characters", nameof(externalId));
        if (title.Length > 500) throw new ArgumentException("Title cannot exceed 500 characters", nameof(title));
        if (description.Length > 2000) throw new ArgumentException("Description cannot exceed 2000 characters", nameof(description));
        if (type.Length > 100) throw new ArgumentException("Type cannot exceed 100 characters", nameof(type));
        if (code.Length > 100) throw new ArgumentException("Code cannot exceed 100 characters", nameof(code));
        if (durationType.Length > 50) throw new ArgumentException("DurationType cannot exceed 50 characters", nameof(durationType));

        ExternalId = externalId;
        Title = title;
        Description = description;
        Type = type;
        Code = code;
        Percentage = percentage;
        DurationDays = durationDays;
        DurationType = durationType;
        SequenceNumber = sequenceNumber;

    }

    /// <summary>
    /// Parent tender
    /// </summary>
    /// <param name="tenderId">Tender identifier</param>
    public void SetTender(Guid tenderId)
    {
        TenderId = tenderId;
    }

}

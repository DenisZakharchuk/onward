// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Generation Stamp: 20260411-7ded7da9
//     Generated: 2026-04-11 15:49:38 UTC
//     Source: procurement.json
//
//     Changes to this file will be lost when regenerated.
//     Custom business logic should be added to separate domain services.
// </auto-generated>

using Onward.Base.Models;

namespace EnergyBrockerage.Prozorro.BL.Entities;

/// <summary>
/// Item offered within a bid, with unit pricing
/// </summary>
public class BidItem : BaseEntity
{
    /// <summary>
    /// Private parameterless constructor for EF Core
    /// </summary>
    private BidItem() { }

    /// <summary>
    /// Creates a new instance of BidItem
    /// </summary>
    /// <param name="externalId">References the corresponding TenderItem external ID</param>
    /// <param name="description">Item description as stated in the bid</param>
    /// <param name="quantity">Quantity offered</param>
    /// <param name="unitName">Unit of measurement name</param>
    /// <param name="unitCode">UN/CEFACT unit code</param>
    /// <param name="unitPrice">Price per unit (net)</param>
    /// <param name="unitPriceCurrency">Currency for the unit price</param>
    /// <param name="bidId">Parent bid</param>
    public BidItem(
        string externalId,
        string description,
        decimal? quantity,
        string unitName,
        string unitCode,
        decimal? unitPrice,
        string unitPriceCurrency,
        Guid bidId
    )
    {
        if (string.IsNullOrWhiteSpace(externalId)) throw new ArgumentException("ExternalId cannot be empty", nameof(externalId));
        if (externalId.Length > 100) throw new ArgumentException("ExternalId cannot exceed 100 characters", nameof(externalId));
        if (description.Length > 2000) throw new ArgumentException("Description cannot exceed 2000 characters", nameof(description));
        if (unitName.Length > 100) throw new ArgumentException("UnitName cannot exceed 100 characters", nameof(unitName));
        if (unitCode.Length > 20) throw new ArgumentException("UnitCode cannot exceed 20 characters", nameof(unitCode));
        if (unitPriceCurrency.Length > 10) throw new ArgumentException("UnitPriceCurrency cannot exceed 10 characters", nameof(unitPriceCurrency));

        ExternalId = externalId;
        Description = description;
        Quantity = quantity;
        UnitName = unitName;
        UnitCode = unitCode;
        UnitPrice = unitPrice;
        UnitPriceCurrency = unitPriceCurrency;
        BidId = bidId;

        Id = Guid.NewGuid();
        CreatedAt = DateTime.UtcNow;
    }

    /// <summary>
    /// References the corresponding TenderItem external ID
    /// </summary>
    public string ExternalId { get; private set; }

    /// <summary>
    /// Item description as stated in the bid
    /// </summary>
    public string Description { get; private set; }

    /// <summary>
    /// Quantity offered
    /// </summary>
    public decimal? Quantity { get; private set; }

    /// <summary>
    /// Unit of measurement name
    /// </summary>
    public string UnitName { get; private set; }

    /// <summary>
    /// UN/CEFACT unit code
    /// </summary>
    public string UnitCode { get; private set; }

    /// <summary>
    /// Price per unit (net)
    /// </summary>
    public decimal? UnitPrice { get; private set; }

    /// <summary>
    /// Currency for the unit price
    /// </summary>
    public string UnitPriceCurrency { get; private set; }

    /// <summary>
    /// Parent bid
    /// </summary>
    public Guid BidId { get; private set; }

    /// <summary>
    /// Navigation to Bid
    /// </summary>
    public Bid Bid { get; } = null!;

    /// <summary>
    /// Timestamp when the entity was created
    /// </summary>
    public DateTime CreatedAt { get; private set; }

    /// <summary>
    /// Updates BidItem with new values
    /// </summary>
    /// <param name="externalId">References the corresponding TenderItem external ID</param>
    /// <param name="description">Item description as stated in the bid</param>
    /// <param name="quantity">Quantity offered</param>
    /// <param name="unitName">Unit of measurement name</param>
    /// <param name="unitCode">UN/CEFACT unit code</param>
    /// <param name="unitPrice">Price per unit (net)</param>
    /// <param name="unitPriceCurrency">Currency for the unit price</param>
    public void Update(
        string externalId,
        string description,
        decimal? quantity,
        string unitName,
        string unitCode,
        decimal? unitPrice,
        string unitPriceCurrency
    )
    {
        if (string.IsNullOrWhiteSpace(externalId)) throw new ArgumentException("ExternalId cannot be empty", nameof(externalId));
        if (externalId.Length > 100) throw new ArgumentException("ExternalId cannot exceed 100 characters", nameof(externalId));
        if (description.Length > 2000) throw new ArgumentException("Description cannot exceed 2000 characters", nameof(description));
        if (unitName.Length > 100) throw new ArgumentException("UnitName cannot exceed 100 characters", nameof(unitName));
        if (unitCode.Length > 20) throw new ArgumentException("UnitCode cannot exceed 20 characters", nameof(unitCode));
        if (unitPriceCurrency.Length > 10) throw new ArgumentException("UnitPriceCurrency cannot exceed 10 characters", nameof(unitPriceCurrency));

        ExternalId = externalId;
        Description = description;
        Quantity = quantity;
        UnitName = unitName;
        UnitCode = unitCode;
        UnitPrice = unitPrice;
        UnitPriceCurrency = unitPriceCurrency;

    }

    /// <summary>
    /// Parent bid
    /// </summary>
    /// <param name="bidId">Bid identifier</param>
    public void SetBid(Guid bidId)
    {
        BidId = bidId;
    }

}

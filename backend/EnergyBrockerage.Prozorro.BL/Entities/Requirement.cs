// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Generation Stamp: 20260411-7ded7da9
//     Generated: 2026-04-11 15:49:38 UTC
//     Source: procurement.json
//
//     Changes to this file will be lost when regenerated.
//     Custom business logic should be added to separate domain services.
// </auto-generated>

using Onward.Base.Models;

namespace EnergyBrockerage.Prozorro.BL.Entities;

/// <summary>
/// Individual requirement within a requirement group, defining expected values for bidder responses
/// </summary>
public class Requirement : BaseEntity
{
    /// <summary>
    /// Private parameterless constructor for EF Core
    /// </summary>
    private Requirement() { }

    /// <summary>
    /// Creates a new instance of Requirement
    /// </summary>
    /// <param name="externalId">Prozorro requirement ID</param>
    /// <param name="title">Requirement title</param>
    /// <param name="dataType">Value data type (string, number, boolean, integer)</param>
    /// <param name="status">Requirement status (active, cancelled)</param>
    /// <param name="expectedValue">Single expected scalar value</param>
    /// <param name="expectedValuesJson">JSON array of allowable expected values for multi-select requirements</param>
    /// <param name="expectedMinItems">Minimum number of matching expected values required</param>
    /// <param name="expectedMaxItems">Maximum number of matching expected values allowed</param>
    /// <param name="datePublished">Date the requirement was published</param>
    /// <param name="requirementGroupId">Parent requirement group</param>
    public Requirement(
        string externalId,
        string title,
        string dataType,
        string status,
        string expectedValue,
        string expectedValuesJson,
        int? expectedMinItems,
        int? expectedMaxItems,
        DateTimeWithOffset? datePublished,
        Guid requirementGroupId
    )
    {
        if (string.IsNullOrWhiteSpace(externalId)) throw new ArgumentException("ExternalId cannot be empty", nameof(externalId));
        if (externalId.Length > 100) throw new ArgumentException("ExternalId cannot exceed 100 characters", nameof(externalId));
        if (title.Length > 500) throw new ArgumentException("Title cannot exceed 500 characters", nameof(title));
        if (dataType.Length > 50) throw new ArgumentException("DataType cannot exceed 50 characters", nameof(dataType));
        if (status.Length > 50) throw new ArgumentException("Status cannot exceed 50 characters", nameof(status));
        if (expectedValue.Length > 500) throw new ArgumentException("ExpectedValue cannot exceed 500 characters", nameof(expectedValue));
        if (expectedValuesJson.Length > 4000) throw new ArgumentException("ExpectedValuesJson cannot exceed 4000 characters", nameof(expectedValuesJson));

        ExternalId = externalId;
        Title = title;
        DataType = dataType;
        Status = status;
        ExpectedValue = expectedValue;
        ExpectedValuesJson = expectedValuesJson;
        ExpectedMinItems = expectedMinItems;
        ExpectedMaxItems = expectedMaxItems;
        DatePublished = datePublished;
        RequirementGroupId = requirementGroupId;

        Id = Guid.NewGuid();
        CreatedAt = DateTime.UtcNow;
    }

    /// <summary>
    /// Prozorro requirement ID
    /// </summary>
    public string ExternalId { get; private set; }

    /// <summary>
    /// Requirement title
    /// </summary>
    public string Title { get; private set; }

    /// <summary>
    /// Value data type (string, number, boolean, integer)
    /// </summary>
    public string DataType { get; private set; }

    /// <summary>
    /// Requirement status (active, cancelled)
    /// </summary>
    public string Status { get; private set; }

    /// <summary>
    /// Single expected scalar value
    /// </summary>
    public string ExpectedValue { get; private set; }

    /// <summary>
    /// JSON array of allowable expected values for multi-select requirements
    /// </summary>
    public string ExpectedValuesJson { get; private set; }

    /// <summary>
    /// Minimum number of matching expected values required
    /// </summary>
    public int? ExpectedMinItems { get; private set; }

    /// <summary>
    /// Maximum number of matching expected values allowed
    /// </summary>
    public int? ExpectedMaxItems { get; private set; }

    /// <summary>
    /// Date the requirement was published
    /// </summary>
    public DateTimeWithOffset? DatePublished { get; private set; }

    /// <summary>
    /// Parent requirement group
    /// </summary>
    public Guid RequirementGroupId { get; private set; }

    /// <summary>
    /// Navigation to RequirementGroup
    /// </summary>
    public RequirementGroup RequirementGroup { get; } = null!;

    /// <summary>
    /// Timestamp when the entity was created
    /// </summary>
    public DateTime CreatedAt { get; private set; }

    /// <summary>
    /// Updates Requirement with new values
    /// </summary>
    /// <param name="externalId">Prozorro requirement ID</param>
    /// <param name="title">Requirement title</param>
    /// <param name="dataType">Value data type (string, number, boolean, integer)</param>
    /// <param name="status">Requirement status (active, cancelled)</param>
    /// <param name="expectedValue">Single expected scalar value</param>
    /// <param name="expectedValuesJson">JSON array of allowable expected values for multi-select requirements</param>
    /// <param name="expectedMinItems">Minimum number of matching expected values required</param>
    /// <param name="expectedMaxItems">Maximum number of matching expected values allowed</param>
    /// <param name="datePublished">Date the requirement was published</param>
    public void Update(
        string externalId,
        string title,
        string dataType,
        string status,
        string expectedValue,
        string expectedValuesJson,
        int? expectedMinItems,
        int? expectedMaxItems,
        DateTimeWithOffset? datePublished
    )
    {
        if (string.IsNullOrWhiteSpace(externalId)) throw new ArgumentException("ExternalId cannot be empty", nameof(externalId));
        if (externalId.Length > 100) throw new ArgumentException("ExternalId cannot exceed 100 characters", nameof(externalId));
        if (title.Length > 500) throw new ArgumentException("Title cannot exceed 500 characters", nameof(title));
        if (dataType.Length > 50) throw new ArgumentException("DataType cannot exceed 50 characters", nameof(dataType));
        if (status.Length > 50) throw new ArgumentException("Status cannot exceed 50 characters", nameof(status));
        if (expectedValue.Length > 500) throw new ArgumentException("ExpectedValue cannot exceed 500 characters", nameof(expectedValue));
        if (expectedValuesJson.Length > 4000) throw new ArgumentException("ExpectedValuesJson cannot exceed 4000 characters", nameof(expectedValuesJson));

        ExternalId = externalId;
        Title = title;
        DataType = dataType;
        Status = status;
        ExpectedValue = expectedValue;
        ExpectedValuesJson = expectedValuesJson;
        ExpectedMinItems = expectedMinItems;
        ExpectedMaxItems = expectedMaxItems;
        DatePublished = datePublished;

    }

    /// <summary>
    /// Parent requirement group
    /// </summary>
    /// <param name="requirementGroupId">RequirementGroup identifier</param>
    public void SetRequirementGroup(Guid requirementGroupId)
    {
        RequirementGroupId = requirementGroupId;
    }

}

// <auto-generated>
//     This code was generated by the BoundedContext Code Generator.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

using EnergyBrockerage.Prozorro.DTO.Organization;
using Onward.Base.Abstractions;

namespace EnergyBrockerage.Prozorro.BL.Validators;

/// <summary>
/// Validator for UpdateOrganizationDTO
/// </summary>
public class UpdateOrganizationValidator : IValidator<UpdateOrganizationDTO>
{
    public Task<ValidationResult> ValidateAsync(
        UpdateOrganizationDTO dto,
        CancellationToken cancellationToken = default)
    {
        if (dto == null)
            return Task.FromResult(ValidationResult.WithErrors("DTO cannot be null"));

        var errors = new List<string>(8);

        if (dto.Id == Guid.Empty)
    errors.Add("Id is required");

        if (string.IsNullOrWhiteSpace(dto.ExternalId))
    errors.Add("ExternalId is required");

        if (!string.IsNullOrEmpty(dto.ExternalId) && dto.ExternalId.Length > 100)
    errors.Add("ExternalId cannot exceed 100 characters");

        if (string.IsNullOrWhiteSpace(dto.IdentifierScheme))
    errors.Add("IdentifierScheme is required");

        if (!string.IsNullOrEmpty(dto.IdentifierScheme) && dto.IdentifierScheme.Length > 50)
    errors.Add("IdentifierScheme cannot exceed 50 characters");

        if (string.IsNullOrWhiteSpace(dto.Name))
    errors.Add("Name is required");

        if (!string.IsNullOrEmpty(dto.Name) && dto.Name.Length > 500)
    errors.Add("Name cannot exceed 500 characters");

        if (!string.IsNullOrEmpty(dto.NameEn) && dto.NameEn.Length > 500)
    errors.Add("NameEn cannot exceed 500 characters");

        if (string.IsNullOrWhiteSpace(dto.LegalName))
    errors.Add("LegalName is required");

        if (!string.IsNullOrEmpty(dto.LegalName) && dto.LegalName.Length > 500)
    errors.Add("LegalName cannot exceed 500 characters");

        if (!string.IsNullOrEmpty(dto.LegalNameEn) && dto.LegalNameEn.Length > 500)
    errors.Add("LegalNameEn cannot exceed 500 characters");

        if (!string.IsNullOrEmpty(dto.StreetAddress) && dto.StreetAddress.Length > 500)
    errors.Add("StreetAddress cannot exceed 500 characters");

        if (!string.IsNullOrEmpty(dto.Locality) && dto.Locality.Length > 200)
    errors.Add("Locality cannot exceed 200 characters");

        if (!string.IsNullOrEmpty(dto.Region) && dto.Region.Length > 200)
    errors.Add("Region cannot exceed 200 characters");

        if (!string.IsNullOrEmpty(dto.PostalCode) && dto.PostalCode.Length > 20)
    errors.Add("PostalCode cannot exceed 20 characters");

        if (!string.IsNullOrEmpty(dto.CountryName) && dto.CountryName.Length > 100)
    errors.Add("CountryName cannot exceed 100 characters");

        if (!string.IsNullOrEmpty(dto.ContactName) && dto.ContactName.Length > 300)
    errors.Add("ContactName cannot exceed 300 characters");

        if (!string.IsNullOrEmpty(dto.ContactEmail) && dto.ContactEmail.Length > 255)
    errors.Add("ContactEmail cannot exceed 255 characters");

        if (!string.IsNullOrEmpty(dto.ContactEmail) && !System.Net.Mail.MailAddress.TryCreate(dto.ContactEmail, out _))
    errors.Add("ContactEmail must be a valid email address");

        if (!string.IsNullOrEmpty(dto.ContactPhone) && dto.ContactPhone.Length > 100)
    errors.Add("ContactPhone cannot exceed 100 characters");

        if (!string.IsNullOrEmpty(dto.ContactUrl) && dto.ContactUrl.Length > 500)
    errors.Add("ContactUrl cannot exceed 500 characters");


        return Task.FromResult(errors.Any()
            ? ValidationResult.WithErrors(errors.ToArray())
            : ValidationResult.Ok());
    }
}
